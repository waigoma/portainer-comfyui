# ユーザーとグループID（ファイル権限の問題を避けるため）
# デフォルトを9001に設定（通常のユーザーID 1000-1999と競合しない）
# 現在のホストユーザーのUID/GIDを使用したい場合：
# USER_ID=$(id -u)
# GROUP_ID=$(id -g)
USER_ID=9001
GROUP_ID=9001

# ComfyUIのポート番号
COMFYUI_PORT=8188

# NVIDIA GPU設定
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility

# Python設定
PYTHONUNBUFFERED=1

# CUDA 13.0用の環境変数
CUDA_VERSION=13.0

# メモリ設定（オプション）
# PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512

# 追加のComfyUI起動オプション（必要に応じて）
# COMFYUI_ARGS=--preview-method auto --use-pytorch-cross-attention
